# Runtime dependencies.
# Plotly is loaded from a CDN (see templates/index.html), so it is intentionally
# NOT a Python dependency. numpy/scipy are pulled in transitively by pandas/sklearn.
flask==3.1.3
pandas==3.0.3
scikit-learn==1.9.0
